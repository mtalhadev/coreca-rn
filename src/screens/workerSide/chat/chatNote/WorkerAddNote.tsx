import React from 'react'
import { StyleSheet } from 'react-native'

import AddNote from '../../../../components/organisms/chat/chatNote/AddNote'

const WorkerAddNote = () => {

    return <AddNote side={'worker'} />
}
export default WorkerAddNote

const styles = StyleSheet.create({})
